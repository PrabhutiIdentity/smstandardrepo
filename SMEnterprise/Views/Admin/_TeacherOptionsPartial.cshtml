
@if (Model.Count == 0)
{

    <option value="0" Selected>No Teacher</option>
}
@{
    var count = 0;
    foreach (var g in Model)
    {
        <option value="@g.EmployeeID" @(count==0?"selected":"")>@g.EmployeeName</option>
        count++;
    }

}