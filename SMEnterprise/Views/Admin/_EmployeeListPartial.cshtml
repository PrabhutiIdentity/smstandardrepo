@model List<SMEnterprise.Models.NameIDModel>
@foreach (var g in Model.Where(x => x.IsSelected != -1).ToList())
{

    <option value="@g.ID" @(g.IsSelected == 1 ? "selected" : "") @(g.IsSelected == -1 ? "disabled" : "")>@g.Name</option>
}