@using SMEnterprise.Models;
@using System.Web.Mvc;
@{
    Layout = "~/Views/Shared/_AccountLayout.cshtml";
    List<NameIDModel> SubjectEvaluationFinalStatus = new List<NameIDModel>();
}
@model SMEnterprise.Models.PerformanceParameterDetailModel

@section IncludeCss
{
    <title>Performance Card | @Model.Student.Name</title>
    <style>

        /* ================================================
           SCREEN STYLES — Optimized for Viewing
           ================================================ */

        .report-wrapper {
            max-width: 210mm; /* A4 width */
            margin: 0 auto;
            background: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 11pt;
            color: #1a1a1a;
            padding: 10mm;
            box-sizing: border-box;
        }

        /* School Header — Table Layout for Print Compatibility */
        .school-header {
            display: table;
            width: 100%;
            border-bottom: 3px double #1a3a6c;
            padding-bottom: 8px;
            margin-bottom: 6px;
        }

        .school-header-left,
        .school-header-right {
            display: table-cell;
            width: 15%;
            vertical-align: middle;
            text-align: center;
        }

        .school-header-center {
            display: table-cell;
            width: 70%;
            vertical-align: middle;
            text-align: center;
            padding: 0 10px;
        }

        .school-logo {
            width: 100px;
            height: 100px;
            object-fit: contain;
            border: 1px solid #ddd;
        }

        .school-name {
            font-size: 22px;
            font-weight: 700;
            color: #0e0e40;
            margin: 0 0 4px 0;
            line-height: 1.2;
            text-transform: uppercase;
        }

        .school-address {
            font-size: 18px;
            color: #153693;
            margin: 3px 0;
            line-height: 1.3;
        }

        .school-contact {
            font-size: 16px;
            color: #333;
            margin: 3px 0;
        }

        /* Report Card Title */
        .report-title-banner {
            text-align: center;
            padding: 8px 0 4px;
            border-bottom: 2px solid #c8ddf5;
            margin-bottom: 8px;
        }

        .rc-label {
            font-size: 20px;
            font-weight: 800;
            color: #98484b;
            letter-spacing: 4px;
            text-transform: uppercase;
            display: block;
        }

        .session-label {
            font-size: 13px;
            color: #2e8fb5;
            font-weight: 600;
            display: block;
            margin-top: 3px;
        }

        /* Student Info Table */
        .student-info-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 5px;
            border: 1.5px solid #b0c4de;
            background: #f8fbff;
        }

            .student-info-table td {
                padding: 4px 8px;
                vertical-align: top;
                font-size: 12px;
                border: none;
            }

        .info-label {
            color: #555;
            font-weight: 500;
        }

        .student-photo {
            width: 100px;
            height: 100px;
            object-fit: cover;
            border: 2px solid #b0c4de;
            background: #fff;
        }

        /* Section Headers */
        .section-header {
            background: linear-gradient(90deg, #1a3a6c 0%, #2e6db4 100%);
            color: #fff !important;
            font-size: 12px;
            font-weight: 700;
            text-align: center;
            padding: 6px 8px;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

            .section-header small {
                font-weight: 400;
                font-size: 10px;
                color: #d0e4ff;
                display: block;
                margin-top: 2px;
            }

        /* Marks Table */
        #tblResult {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
            table-layout: fixed;
        }

            #tblResult th, #tblResult td {
                border: 1px solid #b8cfe8;
                padding: 3px 4px;
                text-align: center;
                vertical-align: middle;
                word-wrap: break-word;
            }

            #tblResult > tbody > tr:first-child td {
                background-color: #1a3a6c !important;
                color: #fff;
                font-weight: 700;
                font-size: 12px;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            #tblResult > tbody > tr:nth-child(2) td {
                background-color: #2e6db4 !important;
                color: #fff;
                font-weight: 600;
                font-size: 12px;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            #tblResult > tbody > tr.subject-row:nth-child(odd) td {
                background-color: #f0f6ff;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            #tblResult > tbody > tr.total-row td {
                background-color: #e8f0fb !important;
                font-weight: 700;
                border-top: 2px solid #1a3a6c;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            #tblResult td:first-child {
                text-align: left !important;
                font-weight: 600;
                width: 12%;
            }

        .fail-mark {
            color: #c0392b;
            font-weight: 700;
        }

        /* Overall Summary Bar */
        .overall-summary {
            display: table;
            width: 100%;
            background: #1a3a6c;
            color: #fff;
            font-size: 11px;
            font-weight: 700;
            margin-bottom: 8px;
            border: 1.5px solid #1a3a6c;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

            .overall-summary > div {
                display: table-cell;
                padding: 6px 8px;
                border-right: 1px solid #4a7ab5;
                text-align: center;
                vertical-align: middle;
                width: 25%;
            }

                .overall-summary > div:last-child {
                    border-right: none;
                }

            .overall-summary span {
                color: #ffd700;
                font-size: 12px;
            }

        /* Co-Scholastic & Param Tables */
        .param-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
        }

            .param-table th, .param-table td {
                border: 1px solid #b8cfe8;
                padding: 4px 6px;
                vertical-align: middle;
            }

            .param-table th {
                background: #2e6db4 !important;
                color: #fff;
                font-weight: 700;
                text-align: left;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            .param-table tr:nth-child(odd) td {
                background: #f5f9ff;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            .param-table tr:nth-child(even) td {
                background: #fff;
            }

        /* Attendance Table */
        .attendance-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
        }

            .attendance-table td {
                border: 1px solid #b8cfe8;
                padding: 4px 6px;
            }


        /* ---------- Grading Scale ---------- */
        /* Grading Scale */
        .grading-scale-box {
            display: table;
            width: 100%;
            margin-top: 6px;
            border: 1px solid #b8cfe8;
        }

        .grade-box {
            display: table-cell;
            width: 50%;
            padding: 6px 8px;
            font-size: 12px;
            background: #f8fbff;
            vertical-align: top;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

            .grade-box:first-child {
                border-right: 1px solid #b8cfe8;
            }

        .grading-scale-box .grade-box b {
            color: #1a3a6c;
        }

        /* ---------- Remarks ---------- */
        .remarks-section {
            margin-top: 6px;
            font-size: 12px;
            border: 1px solid #b8cfe8;
            padding: 5px 10px;
            min-height: 36px;
        }

        /* ---------- Signatures ---------- */
        .signature-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 14px;
        }

            .signature-table td {
                text-align: center;
                padding: 3px 6px;
                vertical-align: bottom;
                font-size: 12px;
                border: none;
            }

        .reportSign {
            max-width: 150px;
            max-height: 60px;
            display: block;
            margin: 0 auto 3px;
        }

        .sig-line {
            border-top: 1px solid #555;
            margin: 4px auto 2px;
            width: 80%;
        }

        /* ---------- Print Button ---------- */
        .print-btn-row {
            margin: 10px 0 4px;
            overflow: hidden;
        }

        /* ================================================
           PRINT  —  A4 one-page optimised
           Uses media print because PrintReport() iframe
           also injects this same CSS block directly.
           ================================================ */
        @@media print {
            @@page {
                size: A4 portrait;
                margin: 8mm 10mm 10mm 10mm;
            }
            /* Hide the entire _AccountLayout shell */
            body > * {
                display: none !important;
            }
            /* Only show the content div that wraps x_panel */
            .right_col {
                display: block !important;
                margin: 0 !important;
                padding: 0 !important;
            }

                .right_col > .page-title,
                .right_col > .top_nav {
                    display: none !important;
                }

            .x_panel {
                display: block !important;
                box-shadow: none !important;
                border: none !important;
                margin: 0 !important;
                padding: 0 !important;
            }

            .x_title {
                display: none !important;
            }

            .x_content {
                display: block !important;
                padding: 0 !important;
            }

            .row, [class*="col-"] {
                display: block !important;
                width: 100% !important;
                margin: 0 !important;
                padding: 0 !important;
            }
            /* Show only report-wrapper */
            .report-wrapper {
                display: block !important;
                max-width: 100% !important;
                padding: 0 !important;
                font-size: 11px;
            }
            /* Hide buttons and inputs */
            .print-btn-row, button, a.btn, input[type="text"], select {
                display: none !important;
            }

            input[type="hidden"] {
                display: none !important;
            }

            .print {
                display: inline !important;
            }

            .noprint {
                display: none !important;
            }
            /* Compact sizing for A4 */
            .school-logo {
                width: 80px !important;
                height: 80px !important;
            }

            .school-name {
                font-size: 18px !important;
            }

            .school-address {
                font-size: 10px !important;
            }

            .school-contact {
                font-size: 10px !important;
            }

            .rc-label {
                font-size: 16px !important;
            }

            .session-label {
                font-size: 11px !important;
            }

            .student-info-table td {
                font-size: 11px !important;
                padding: 3px 8px !important;
            }

            .student-photo {
                width: 70px !important;
                height: 70px !important;
            }

            #tblResult, #tblResult td, #tblResult th {
                font-size: 9px !important;
                padding: 2px 3px !important;
            }

            .param-table, .param-table td, .param-table th {
                font-size: 9px !important;
                padding: 2px 4px !important;
            }

            .overall-summary {
                font-size: 10px !important;
            }

                .overall-summary > div {
                    padding: 3px 4px !important;
                }

            .remarks-section {
                min-height: 24px !important;
                font-size: 10px !important;
            }

            .grading-scale-box .grade-box {
                font-size: 9px !important;
                padding: 3px 5px !important;
            }

            .signature-table td {
                font-size: 10px !important;
            }

            .reportSign {
                max-height: 50px !important;
            }

            .section-header {
                padding: 3px 6px !important;
                font-size: 11px !important;
            }
            /* Colour backgrounds must print */
            .section-header,
            #tblResult > tbody > tr:first-child td,
            #tblResult > tbody > tr:nth-child(2) td,
            #tblResult > tbody > tr.total-row td,
            .overall-summary,
            .param-table th {
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
                color-adjust: exact !important;
            }

            table {
                page-break-inside: auto;
            }

            tr {
                page-break-inside: avoid;
                page-break-after: auto;
            }
        }
    </style>
}
<div class="report-wrapper">

    @using (Html.BeginForm("UpdateStudentPerformance", "Account", FormMethod.Post,
new { role = "form", enctype = "multipart/form-data", @ID = "ClassUpdate" }))
    {
        <!-- ===== SCHOOL HEADER ===== -->
        <div class="school-header">
            <div class="school-header-left">
                <img onerror="return OnImageError(this,2);"
                     class="school-logo" alt="School Logo"
                     src="@Url.Content("~/Images/SBranchLogo/")@(Model.SBranchDetails.SBranchID)_@(Model.SBranchDetails.Logo)" />
            </div>

            <div class="school-header-center">
                <p class="school-name">@Model.SBranchDetails.BranchSchoolName</p>
                <p class="school-address">
                    @Html.Raw(Html.Encode(Model.SBranchDetails.Address).Replace("\n", "<br />"))
                </p>
                <p class="school-contact">
                    <b>Ph.</b> @Model.SBranchDetails.ContactNo &nbsp;|&nbsp;
                    <b>Email:</b> @Model.SBranchDetails.EmailID
                </p>
            </div>

            <div class="school-header-right">
                @if (Model.SBranchDetails.SBranchID == 23)
                {
                    <img onerror="return OnImageError(this,2);"
                         class="school-logo" alt="CBSE Logo"
                         src="@Url.Content("~/Images/cbse-logo.png")" />
                }
                else if (Model.SBranchDetails.SBranchID != 17)
                {
                    <img onerror="return OnImageError(this,2);"
                         class="school-logo" alt="Logo"
                         src="@Url.Content("~/Images/SBranchLogo/")@(Model.SBranchDetails.SBranchID)_@(Model.SBranchDetails.Logo)" />
                }
            </div>
        </div>

        <!-- ===== REPORT CARD TITLE ===== -->
        <div class="report-title-banner">
            <div class="rc-label">&#9670; Report Card &#9670;</div>
            <div class="session-label">Academic Session : @Model.SessionName</div>
        </div>

        <!-- ===== STUDENT INFO ===== -->
        <table class="student-info-table">
            <tr>
                <td style="width:44%">
                    <table style="width:100%;border-collapse:collapse;">
                        <tr><td><span class="info-label">Roll No.&nbsp;:</span> <b>@Model.Student.RollNo</b></td></tr>
                        <tr><td><span class="info-label">Student&nbsp;:</span> <b id="bStudentNate">@Model.Student.Name</b></td></tr>
                        <tr><td><span class="info-label">Mother's Name&nbsp;:</span> <b>@Model.Student.MotherName</b></td></tr>
                        <tr><td><span class="info-label">Father's Name&nbsp;:</span> <b>@Model.Student.FatherName</b></td></tr>
                        <tr><td><span class="info-label">Parent's ID&nbsp;:</span> <b>@Model.Student.SParentID</b></td></tr>
                    </table>
                </td>
                <td style="width:44%">
                    <table style="width:100%;border-collapse:collapse;">
                        <tr><td><span class="info-label">Date of Birth&nbsp;:</span> <b>@Model.Student.DOB.ToString("dd MMM, yyyy")</b></td></tr>
                        <tr><td><span class="info-label">Class-Section&nbsp;:</span> <b id="bClassSection">@Model.Student.ClassName</b></td></tr>
                        <tr><td><span class="info-label">Student ID&nbsp;:</span> <b>@Model.Student.StudentSID</b></td></tr>
                        <tr><td><span class="info-label">SR No.&nbsp;:</span> <b>@Model.Student.SchoolUID</b></td></tr>
                    </table>
                </td>
                <td style="width:12%;text-align:center;vertical-align:middle;">
                    @if (Model.Student.Photo != null)
                    {
                        <img id="imgPhoto"
                             onerror="return OnImageError(this,@(Model.Student.Gender));"
                             class="student-photo" alt="Student Photo"
                             src="@Url.Content("~/Images/StudentImage/")@(Model.Student.StudentID)_@Model.Student.Photo" />
                    }
                </td>
            </tr>
        </table>

        <!-- ===== HIDDEN FORM FIELDS ===== -->
        <input type="hidden" name="StudentSessionUID" value="@Model.StudentSessionUID" />
        <input type="hidden" name="EvaluationID" value="@Model.EvaluationID" />

        <!-- ===== PERFORMANCE PARAMETERS LOOP ===== -->

        var mainCounter = 0;


        <!-- Main marks table (ParamType 0) -->
        foreach (PerformanceParameterModel c in Model.PerformanceParameters)
        {
            if (c.ParamType == 0)
            {
                <table style="width:100%;border-collapse:collapse;border:1.5px solid #b0c4de;margin-bottom:12px;">
                    <tr>
                        <td class="section-header">
                            @c.ParamName
                            @if (!string.IsNullOrEmpty(c.ParamDesc))
                            {
                                <small>@c.ParamDesc</small>
                            }
                        </td>
                    </tr>
                    <tr>
                        <td style="padding:0;">
                            <table id="tblResult">
                                @{
                                    var sublevels = 1;

                                    <!-- Header Row 1: Evaluation names -->
                                    <tr>
                                        <td @(Model.SubEvaluations.Count > 0 ? "rowspan=2" : "") style="text-align:left!important;">
                                            <b>Subject</b>
                                        </td>
                                        @foreach (EvaluationModel me in Model.MainEvaluations)
                                        {
                                            if (sublevels < me.SubEvals) { sublevels = me.SubEvals; }
                                            <td colspan="@(me.SubEvals + 2)"><b>@me.EvaluationName</b> (@me.Weightage.ToString("0") marks)</td>
                                        }
                                        @if (Model.EvaluationID == -1)
                                        {
                                            <td colspan="2"><b>Sub Total</b></td>
                                        }
                                    </tr>

                                    <!-- Header Row 2: Sub-evaluation columns -->
                                    if (Model.SubEvaluations.Count > 0)
                                    {
                                        <tr>
                                            @foreach (EvaluationModel tme in Model.MainEvaluations)
                                            {
                                                foreach (EvaluationModel se in Model.SubEvaluations)
                                                {
                                                    if (se.MasterID == tme.EvaluationID)
                                                    {
                                                        <td><b>@se.EvaluationName</b> (@se.Weightage)</td>
                                                    }
                                                }
                                                <td><b>Marks Obtained (@tme.Weightage)</b></td>
                                                <td><b>Grade</b></td>
                                            }
                                            @if (Model.EvaluationID == -1)
                                            {
                                                <td><b>Marks Obtained</b><br />(@Model.MainEvaluations.Sum(x => x.Weightage))</td>
                                                <td><b>Grade</b></td>
                                            }
                                        </tr>
                                    }

                                    <!-- Data Rows -->
                                    var privSubID = 0;
                                    decimal totalmarksresult = 0;
                                    var division = "First";
                                    var Grade = "A";
                                    var GradeAll = "A";
                                    decimal TotalMarksT = 0;
                                    decimal TotalObtMarksT = 0;
                                    var divisionAll = "First";
                                    decimal totaltotal = 0;
                                    decimal totalscored = 0;

                                    foreach (ExamResultDetailModel r in Model.Result)
                                    {
                                        if (privSubID != r.SubjectID)
                                        {
                                            <tr class="subject-row">
                                                <td><b>@r.SubjectName</b></td>
                                                @foreach (EvaluationModel er in Model.MainEvaluations)
                                                {
                                                    decimal EvalMaxMarks = 0;
                                                    decimal SATotalMarks = 0;
                                                    decimal EvalTotalMarks = 0;

                                                    foreach (EvaluationModel esr in Model.SubEvaluations)
                                                    {
                                                        if (esr.MasterID == er.EvaluationID)
                                                        {
                                                            var isFound = false;
                                                            foreach (ExamResultDetailModel err in Model.Result)
                                                            {
                                                                if (err.MasterID == er.EvaluationID && esr.EvaluationType == err.EvaluationID && r.SubjectID == err.SubjectID)
                                                                {
                                                                    if (Model.SBranchID == 0)
                                                                    {
                                                                        <td>@err.wMaxMarks.ToString("N2")</td>
                                                                    }
                                                                    else
                                                                    {
                                                                        if (err.Status == -1)
                                                                        {
                                                                            <td>M/L</td>
                                                                        }
                                                                        else
                                                                        {
                                                                            <td class="@(err.wMarksScored < err.PassMarks ? "fail-mark" : "")">
                                                                                @err.wMarksScored.ToString("0")
                                                                            </td>
                                                                        }
                                                                    }
                                                                    EvalTotalMarks += err.wMarksScored;
                                                                    EvalMaxMarks += err.wMaxMarks;
                                                                    er.TotalMarks += err.wMaxMarks;
                                                                    er.MarksObtained += err.wMarksScored;
                                                                    r.etMarksScored += err.wMarksScored;
                                                                    r.etMaxMarks += err.wMaxMarks;
                                                                    isFound = true;
                                                                }
                                                            }
                                                            if (!isFound)
                                                            {
                                                                <td>NA</td>
                                                            }
                                                        }
                                                    }

                                                    var subEve = SubjectEvaluationFinalStatus.Where(se => se.ID == r.SubjectID && se.Name == er.EvaluationID.ToString()).SingleOrDefault();
                                                    if (subEve == null)
                                                    {
                                                        subEve = new NameIDModel();
                                                        subEve.ID = r.SubjectID;
                                                        subEve.Name = er.EvaluationID.ToString();
                                                        if (EvalMaxMarks > 0)
                                                        {

                                                            subEve.Extra1 = (EvalTotalMarks * 100 / EvalMaxMarks).ToString();
                                                        }
                                                        @* subEve.Extra1 = (EvalTotalMarks * 100 / EvalMaxMarks).ToString();*@
                                                        SubjectEvaluationFinalStatus.Add(subEve);
                                                    }
                                                    else
                                                    {
                                                        subEve.Extra1 = (EvalTotalMarks * 100 / EvalMaxMarks).ToString();
                                                    }

                                                    totalmarksresult += EvalMaxMarks;
                                                    <td>@Math.Round(EvalTotalMarks, 2)</td>
                                                    <td>@AcadmicModel.CalculateGrade(EvalTotalMarks, EvalMaxMarks)</td>
                                                }
                                                @if (Model.EvaluationID == -1)
                                                {
                                                    <td>@r.etMarksScored.ToString("0.00")</td>
                                                    <td>@AcadmicModel.CalculateGrade(r.etMarksScored, r.etMaxMarks)</td>
                                                }
                                            </tr>
                                        }
                                        privSubID = r.SubjectID;
                                    }

                                    <!-- Total Row -->
                                    <tr class="total-row">
                                        <td style="text-align:left!important;"><b>Total</b></td>
                                        @foreach (var me in Model.MainEvaluations)
                                        {
                                            <td colspan="@(me.SubEvals)"><b>Total</b></td>
                                            <td><b>@Math.Round(me.MarksObtained, 2)</b></td>
                                            division = @AcadmicModel.CalculateGradeDivision(me.MarksObtained, me.TotalMarks);
                                            TotalMarksT = @Math.Round(me.TotalMarks, 2);
                                            TotalObtMarksT = @Math.Round(me.MarksObtained, 2);
                                            Grade = @AcadmicModel.CalculateGrade(me.MarksObtained, me.TotalMarks);
                                            <td><b>@Math.Round(me.TotalMarks == 0 ? 0 : me.MarksObtained * 100 / me.TotalMarks, 2)% (@AcadmicModel.CalculateGrade(me.MarksObtained, me.TotalMarks))</b></td>
                                        }
                                        @if (Model.EvaluationID == -1)
                                        {
                                            <td>@Model.Result.Sum(x => x.etMarksScored).ToString("0.00")</td>
                                            totaltotal = Model.Result.Sum(x => x.etMaxMarks);
                                            totalscored = Model.Result.Sum(x => x.etMarksScored);
                                            divisionAll = @AcadmicModel.CalculateGradeDivision(totalscored, totaltotal);
                                            GradeAll = @AcadmicModel.CalculateGrade(totalscored, totaltotal);
                                            <td>@Math.Round(totaltotal == 0 ? 0 : totalscored * 100 / totaltotal, 2)% (@AcadmicModel.CalculateGrade(totalscored, totaltotal))</td>
                                        }
                                    </tr>
                                }
                            </table>
                        </td>
                    </tr>
                </table>

                <!-- Overall Summary Bar -->
                <div class="overall-summary" style="margin-bottom:12px;">
                    @if (Model.EvaluationID == -1)
                    {
                        <div>Overall Marks : <span>@totalscored.ToString("0.00") / @totaltotal.ToString("0.00")</span></div>
                        <div>Percentage : <span>@Math.Round(totaltotal == 0 ? 0 : totalscored * 100 / totaltotal, 2).ToString("0.00")%</span></div>
                        <div>Grade : <span>@GradeAll</span></div>
                        <div>Division : <span>@divisionAll</span></div>
                    }
                    else
                    {
                        <div>Overall Marks : <span>@TotalObtMarksT.ToString("0.00") / @TotalMarksT.ToString("0.00")</span></div>
                        <div>Percentage : <span>@Math.Round(TotalMarksT == 0 ? 0 : TotalObtMarksT * 100 / TotalMarksT, 2).ToString("0.00")%</span></div>
                        <div>Grade : <span>@Grade</span></div>
                        <div>Division : <span>@division</span></div>
                    }
                </div>
            }

            <!-- ===== CO-SCHOLASTIC (ParamType 1) ===== -->
            if (c.ParamType == 1)
            {
                <table style="width:100%;border-collapse:collapse;margin-bottom:8px;">
                    <tr>
                        @foreach (EvaluationModel eprop in Model.MainEvaluations)
                        {
                            <td style="width:@(100 / Model.MainEvaluations.Count)%;vertical-align:top;padding:0 4px 0 0;">
                                <table class="param-table">
                                    @if (c.ParamName != "")
                                    {
                                        <tr>
                                            <th colspan="2">
                                                @Html.Raw(c.ParamName.Replace("[EvaluationName]", eprop.EvaluationName))
                                            </th>
                                            <th>Grade</th>
                                        </tr>
                                    }
                                    @if (c.Subs != 0)
                                    {
                                        foreach (PerformanceParameterModel s in Model.PerformanceParameters)
                                        {
                                            if ((s.ParamType == 5) && s.MainParamID == c.ParamID && (s.EvaluationID == eprop.EvaluationID || s.EvaluationID == -1))
                                            {
                                                <tr>
                                                    <td colspan="2">@Html.Raw(s.ParamName.Replace("[EvaluationName]", eprop.EvaluationName))</td>
                                                    <td>
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamKey" value="@s.ParamKey" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamID" value="@s.ParamID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamValID" value="@s.ParamValID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].EvaluationID" value="@eprop.EvaluationID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].StudentSessionID" value="@Model.StudentSessionUID" />
                                                        @if (Model.EvaluationID != -1)
                                                        {
                                                            <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].Grade" value="@s.Grade" />
                                                            <span class="print">@s.Grade</span>
                                                        }
                                                        else
                                                        { @s.Grade}
                                                    </td>
                                                </tr>
                                                mainCounter++;
                                            }
                                            if ((s.ParamType == 6) && s.MainParamID == c.ParamID && (s.EvaluationID == eprop.EvaluationID || s.Grade == null))
                                            {
                                                <tr>
                                                    <td style="width:60%"><b>@Html.Raw(s.ParamName.Replace("[EvaluationName]", eprop.EvaluationName))</b></td>
                                                    <td>
                                                        Marks:
                                                        <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].ParamValue" value="@s.ParamValue" />
                                                        <span class="print">@s.ParamValue</span>
                                                    </td>
                                                    <td>
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamKey" value="@s.ParamKey" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamID" value="@s.ParamID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamValID" value="@s.ParamValID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].EvaluationID" value="@eprop.EvaluationID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].StudentSessionID" value="@Model.StudentSessionUID" />
                                                        Grade:
                                                        @if (Model.EvaluationID != -1)
                                                        {
                                                            <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].Grade" value="@s.Grade" />
                                                            <span class="print">@s.Grade</span>
                                                        }
                                                        else
                                                        { @s.Grade}
                                                    </td>
                                                </tr>
                                                mainCounter++;
                                            }
                                            if ((s.ParamType == 7) && s.MainParamID == c.ParamID && (s.EvaluationID == eprop.EvaluationID || s.Grade == null))
                                            {
                                                <tr>
                                                    <td colspan="2"><b>@Html.Raw(s.ParamName.Replace("[EvaluationName]", eprop.EvaluationName))</b></td>
                                                    <td>
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamKey" value="@s.ParamKey" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamID" value="@s.ParamID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].ParamValID" value="@s.ParamValID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].EvaluationID" value="@eprop.EvaluationID" />
                                                        <input type="hidden" name="PerformanceParameters[@mainCounter].StudentSessionID" value="@Model.StudentSessionUID" />
                                                        @if (Model.EvaluationID != -1)
                                                        {
                                                            <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].Grade" value="@s.Grade" />
                                                            <span class="print">@s.Grade</span>
                                                        }
                                                        else
                                                        { @s.Grade}
                                                    </td>
                                                </tr>
                                                mainCounter++;
                                            }
                                        }
                                    }
                                </table>
                            </td>
                        }
                    </tr>
                </table>
            }

            <!-- ===== ATTENDANCE (ParamType 3 / attendance inputs) ===== -->
            if (c.ParamType == 3)
            {
                <table style="width:100%;border-collapse:collapse;margin-bottom:8px;">
                    <tr>
                        @foreach (EvaluationModel eprop in Model.MainEvaluations)
                        {
                            <td style="width:@(100 / Model.MainEvaluations.Count)%;vertical-align:top;padding:0 4px 0 0;">
                                <table class="attendance-table">
                                    @if (c.ParamName != "")
                                    {
                                        <tr style="background:#2e6db4;color:#fff;">
                                            <td colspan="2"><b>@Html.Raw(c.ParamName.Replace("[EvaluationName]", eprop.EvaluationName))</b></td>
                                        </tr>
                                    }
                                    @foreach (PerformanceParameterModel s in Model.PerformanceParameters)
                                    {
                                        if (s.ParamType == 4 && s.MainParamID == c.ParamID && (s.EvaluationID == eprop.EvaluationID || s.EvaluationID == -1))
                                        {
                                            <tr>
                                                <td><b>@s.ParamName</b></td>
                                                <td>
                                                    Attendance:
                                                    <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].ParamValue" value="@s.ParamValue" />
                                                    <span class="print">@s.ParamValue</span>
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].ParamKey" value="@s.ParamKey" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].ParamID" value="@s.ParamID" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].ParamValID" value="@s.ParamValID" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].EvaluationID" value="@eprop.EvaluationID" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].StudentSessionID" value="@Model.StudentSessionUID" />
                                                    Working Days:
                                                    <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].Grade" value="@s.Grade" />
                                                    <span class="print">@s.Grade</span>
                                                    mainCounter++;
                                                </td>
                                            </tr>
                                        }
                                    }
                                </table>
                            </td>
                        }
                    </tr>
                </table>
            }

            <!-- ===== TYPE 2 — Theory/Practical table ===== -->
            if (c.ParamType == 2)
            {
                <table style="width:100%;border-collapse:collapse;margin-bottom:8px;">
                    <tr>
                        @foreach (EvaluationModel eprop in Model.MainEvaluations)
                        {
                            <td style="width:@(100 / Model.MainEvaluations.Count)%;vertical-align:top;padding:0 4px 0 0;">
                                <table class="param-table">
                                    @if (c.ParamName != "")
                                    {
                                        <tr><th colspan="3">@Html.Raw(c.ParamName.Replace("[EvaluationName]", eprop.EvaluationName))</th></tr>
                                    }
                                    <tr>
                                        <th>Subject</th>
                                        <th>Theory</th>
                                        <th>Practical</th>
                                    </tr>

                                    var oldParamID = 0;
                                    @foreach (PerformanceParameterModel s in Model.PerformanceParameters)
                                    {
                                        if ((s.ParamType == 8) && s.MainParamID == c.ParamID && (s.EvaluationID == eprop.EvaluationID || s.EvaluationID == -1))
                                        {
                                            <tr>
                                                <td><b>@s.ParamName</b></td>
                                                <td>
                                                    <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].ParamValue" value="@s.ParamValue" />
                                                    <span class="print">@s.ParamValue</span>
                                                </td>
                                                <td>
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].ParamKey" value="@s.ParamKey" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].ParamID" value="@s.ParamID" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].ParamValID" value="@s.ParamValID" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].EvaluationID" value="@eprop.EvaluationID" />
                                                    <input type="hidden" name="PerformanceParameters[@mainCounter].StudentSessionID" value="@Model.StudentSessionUID" />
                                                    <input type="text" class="form-control noprint" name="PerformanceParameters[@mainCounter].Grade" value="@s.Grade" />
                                                    <span class="print">@s.Grade</span>
                                                </td>
                                            </tr>
                                            mainCounter++;
                                        }
                                    }

                                </table>
                            </td>
                        }
                    </tr>
                </table>
            }
        }


        <!-- ===== RESULT STATUS ===== -->
        if (Model.MainEvaluations.Count > 1)
        {
            var finalStatus = 0;
            foreach (var se in SubjectEvaluationFinalStatus)
            {
                if (Convert.ToDecimal(se.Extra1) < 40)
                {
                    var subsum = SubjectEvaluationFinalStatus.Where(s => s.ID == se.ID).Sum(c2 => Convert.ToDecimal(c2.Extra1));
                    var subsumcount = SubjectEvaluationFinalStatus.Where(s => s.ID == se.ID).Count();
                    if (subsum / subsumcount < 40) { finalStatus = 1; break; }
                    else if (subsum / subsumcount > 40) { finalStatus = 2; break; }
                }
            }
            if (Model.SBranchDetails.SBranchID != 9)
            {
                <div style="border:1.5px solid #1a3a6c;border-radius:4px;padding:6px 12px;margin-bottom:8px;font-size:13px;background:#f8fbff;">
                    <b>Result Status :</b>
                    <b style="color:@(finalStatus == 0 ? "#1a7a1a" : "#c0392b")">
                        @(finalStatus == 0 ? "Pass" : finalStatus == 1 ? "Pass" : "Pass")
                    </b>
                </div>
            }

        }

        <!-- ===== REMARKS ===== -->
        <div class="remarks-section">
            <b>Class Teacher's Remarks :</b>
            <br /><br />
        </div>

        <!-- ===== GRADING SCALE ===== -->
        <div class="grading-scale-box">
            <div class="grade-box">
                <b>Grading Scale &ndash; Scholastic Areas</b><br />
                Grades are awarded on an 8-point scale:<br />
                <b>A1</b> [91-100%] &nbsp; <b>A2</b> [81-90%] &nbsp; <b>B1</b> [71-80%] &nbsp; <b>B2</b> [61-70%]<br />
                <b>C1</b> [51-60%] &nbsp; <b>C2</b> [41-50%] &nbsp; <b>D</b> [33-40%] &nbsp; <b>E</b> [≤32% - Needs Improvement]
            </div>
            <div class="grade-box">
                <b>Grading Scale &ndash; Co-Scholastic Areas</b><br />
                Grades are awarded on a 3-point scale:<br />
                <b>A</b> [Outstanding] &nbsp; <b>B</b> [Very Good] &nbsp; <b>C</b> [Fair] &nbsp; <b>D</b> [Needs Improvement]
            </div>
        </div>

        <!-- ===== SIGNATURES ===== -->
        <table class="signature-table" style="margin-top:20px;">
            <tr>
                <td style="width:25%;text-align:left;vertical-align:bottom;font-size:12px;">
                    Place : <br />
                    Date &nbsp;: @Model.MainEvaluations[Model.MainEvaluations.Count - 1].ResultDate.ToString("dd MMM, yyyy")
                </td>
                <td style="width:25%">
                    @if (Model.SBranchDetails.ClassTeacherSign != null)
                    {
                        <img onerror="return OnImageError(this,2);" class="reportSign" alt="Class Teacher Sign"
                             src="@Url.Content("~/Images/EmployeeDocuments/")@(Model.SBranchDetails.CTeacherID)_Sign_@(Model.SBranchDetails.ClassTeacherSign)" />
                    }
                    <div class="sig-line"></div>
                    @(Model.SBranchDetails.ClassTeacher ?? "") <br />(Class Teacher)
                </td>
                <td style="width:25%">
                    @if (Model.SBranchDetails.TeacherIncharge != null && Model.SBranchDetails.TeacherInchargeSign != null)
                    {
                        <img onerror="return OnImageError(this,2);" class="reportSign" alt="Teacher Incharge Sign"
                             src="@Url.Content("~/Images/EmployeeDocuments/")@(Model.SBranchDetails.InchargeTeacherID)_Sign_@(Model.SBranchDetails.TeacherInchargeSign)" />
                    }
                    <div class="sig-line"></div>
                    @(Model.SBranchDetails.TeacherIncharge ?? "") <br />(Teacher Incharge)
                </td>
                <td style="width:25%">
                    @if (Model.SBranchDetails.PrincipalSignature != null)
                    {
                        <img onerror="return OnImageError(this,2);" class="reportSign" alt="Principal Sign"
                             src="@Url.Content("~/Images/SBranchLogo/")@(Model.SBranchDetails.SBranchID)_P_@(Model.SBranchDetails.PrincipalSignature)" />
                    }
                    <div class="sig-line"></div>
                    Signature of Principal
                </td>
            </tr>
        </table>



        <input type="hidden" id="hdnCounterCount" value="@mainCounter" />
    }
    <div class="print-btn-row">
        <a class="btn btn-success" href="javascript:void(0);" onclick="PrintReport();">🖨 Print Report</a>
        @if (Model.EvaluationID != -1)
        {
            <button class="btn btn-primary" type="submit" onclick="$(this).css('display','none');" value="Save">💾 Save</button>
        }
    </div>

</div>
<!-- ===== ACTION BUTTONS (screen only) ===== -->



@section scripts
{
    <script type="text/javascript">
        function PrintReport() {
            var StudentName = $('#bStudentNate').html();
            var ClassSection = $('#bClassSection').html();
            var contents = $(".report-wrapper").html();
            var frame1 = $('<iframe />');
            frame1[0].name = "frame1";
            frame1.css({ "position": "absolute", "top": "-1000000px" });
            $("body").append(frame1);
            var frameDoc = frame1[0].contentWindow
                ? frame1[0].contentWindow
                : (frame1[0].contentDocument.document || frame1[0].contentDocument);

            var css = [
                '@@page{size:A4 portrait;margin:8mm 10mm 10mm 10mm;}',
                '*{box-sizing:border-box;}',
                'body{font-family:"Segoe UI",Arial,sans-serif;font-size:11px;color:#1a1a1a;margin:0;padding:0;background:#fff;}',
                'table{border-collapse:collapse;width:100%;}',
                'td,th{border:1px solid #b8cfe8;padding:3px 4px;vertical-align:middle;}',
                /* hide buttons/inputs, show .print spans */
                '.print-btn-row,.noprint,input,select,button,.btn{display:none!important;}',
                '.print{display:inline!important;}',
                /* school header — table layout avoids flex issues in iframe */
                '.school-header{display:table;width:100%;border-bottom:3px double #1a3a6c;padding-bottom:6px;margin-bottom:4px;}',
                '.school-header-left,.school-header-right{display:table-cell;width:13%;vertical-align:middle;text-align:center;}',
                '.school-header-center{display:table-cell;width:74%;vertical-align:middle;text-align:center;padding:0 8px;}',
                '.school-logo{width:120px;height:120px;object-fit:contain;}',
                '.school-name{font-size:29px;font-weight:700;color:#0e0e40;margin:0 0 3px;}',
                '.school-address{font-size:18px;color:#153693;margin:2px 0;}',
                '.school-contact{font-size:16px;color:#333;margin:2px 0;}',
                /* title */
                '.report-title-banner{text-align:center;padding:4px 0 2px;border-bottom:2px solid #c8ddf5;margin-bottom:5px;}',
                '.rc-label{font-size:16px;font-weight:800;color:#98484b;letter-spacing:3px;display:block;}',
                '.session-label{font-size:11px;color:#2e8fb5;font-weight:600;display:block;}',
                /* student info */
                '.student-info-table{border:1.5px solid #b0c4de;background:#f8fbff!important;margin-bottom:5px;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '.student-info-table td{border:0;padding:3px 5px;font-size:12px;}',
                '.student-photo{width:72px;height:72px;object-fit:cover;border:2px solid #b0c4de;}',
                /* section header */
                '.section-header{background:#1a3a6c!important;color:#fff!important;font-weight:700;text-align:center;padding:4px 6px;font-size:11px;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '.section-header small{color:#d0e4ff;font-size:12px;display:block;}',
                /* marks table */
                '#tblResult{width:100%;border-collapse:collapse;font-size:12px;}',
                '#tblResult td,#tblResult th{border:1px solid #b8cfe8;padding:2px 3px;text-align:center;}',
                '#tblResult td:first-child{text-align:left!important;font-weight:600;}',
                '#tblResult>tbody>tr:first-child td{background:#1a3a6c!important;color:#fff!important;font-weight:700;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '#tblResult>tbody>tr:nth-child(2) td{background:#2e6db4!important;color:#fff!important;font-size:12px;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '#tblResult>tbody>tr.subject-row:nth-child(odd) td{background:#f0f6ff!important;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '#tblResult>tbody>tr.total-row td{background:#e8f0fb!important;font-weight:700;border-top:2px solid #1a3a6c;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '.fail-mark{text-decoration:underline;color:#c0392b;font-weight:700;}',
                /* overall summary — table display for iframe compat */
                '.overall-summary{display:table;width:100%;background:#1a3a6c!important;color:#fff!important;font-weight:700;font-size:12px;margin-bottom:6px;border:1.5px solid #1a3a6c;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '.overall-summary>div{display:table-cell;padding:3px 5px;border-right:1px solid #4a7ab5;text-align:center;vertical-align:middle;}',
                '.overall-summary>div:last-child{border-right:none;}',
                '.overall-summary span{color:#ffd700;}',
                /* param / co-scholastic */
                '.param-table{width:100%;border-collapse:collapse;font-size:12px;}',
                '.param-table th{background:#2e6db4!important;color:#fff!important;padding:3px 5px;text-align:left;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '.param-table td{padding:2px 5px;border:1px solid #b8cfe8;}',
                '.param-table tr:nth-child(odd) td{background:#f5f9ff!important;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                /* attendance */
                '.attendance-table{width:100%;border-collapse:collapse;font-size:12px;}',
                '.attendance-table td{border:1px solid #b8cfe8;padding:2px 5px;}',
                /* grading scale */
                '.grading-scale-box{display:table;width:100%;margin-top:5px;}',
                '.grade-box{display:table-cell;width:50%;border:1px solid #b8cfe8;padding:4px 6px;font-size:12px;background:#f8fbff!important;vertical-align:top;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                '.grade-box:first-child{border-right:none;}',
                '.grade-box b{color:#1a3a6c;}',
                /* remarks */
                '.remarks-section{border:1px solid #b8cfe8;padding:4px 8px;min-height:24px;font-size:12px;margin:4px 0;}',
                /* result status */
                '.result-status-box{border:1.5px solid #1a3a6c;padding:4px 8px;margin-bottom:4px;font-size:11px;background:#f8fbff!important;-webkit-print-color-adjust:exact;print-color-adjust:exact;}',
                /* signatures */
                '.signature-table{width:100%;border-collapse:collapse;margin-top:12px;}',
                '.signature-table td{text-align:center;padding:2px 5px;vertical-align:bottom;font-size:10px;border:0;}',
                '.reportSign{max-width:120px;max-height:50px;display:block;margin:0 auto 2px;}',
                '.sig-line{border-top:1px solid #555;margin:3px auto 2px;width:80%;}',
                /* page break */
                'table{page-break-inside:auto;}',
                'tr{page-break-inside:avoid;page-break-after:auto;}'
            ].join('');

            frameDoc.document.open();
            frameDoc.document.write(
                '<!DOCTYPE html><html><head>' +
                '<meta charset="utf-8" />' +
                '<title>Report Card - ' + StudentName + ' | ' + ClassSection + '</title>' +
                '<style>' + css + '</style>' +
                '</head><body>' + contents + '</body></html>'
            );
            frameDoc.document.close();
            setTimeout(function () {
                window.frames["frame1"].focus();
                window.frames["frame1"].print();
                frame1.remove();
            }, 700);
        }
    </script>
}
